# Photoshop AI Plugin 开发约定

这是独立项目。先读 [当前范围](docs/plans/00-scope.md)；开发顺序见 [路线图](docs/plans/01-roadmap.md)。

## Issue 驱动

1. 开始实现前确认对应 GitHub Issue 的目标、依赖和验收条件。
2. 分支命名 `feat/<编号>-<主题>`、`fix/<编号>-<主题>` 或 `docs/<编号>-<主题>`。提交信息包含 `refs #<编号>`。
3. PR 写清实际行为、验证证据和剩余阻碍。部分实现用 `Refs #N`；满足全部验收后才用 `Closes #N`。初始仓库提交直接建立 main，其余变化通过可检查分支记录。
4. Issue 的状态以真实证据为准。环境阻碍、缺少凭据、模型权限和接口冲突都保留为未完成，不能用 Mock 结果关闭真实宿主/模型验收。
5. 修改范围或开发路径时同步更新对应计划和 Issue；原始设计文件保留作为历史依据。

## 按任务读取

- 模型与请求字段：读 [Cangyuan 能力表](docs/plans/02-cangyuan.md)。
- Photoshop 读写或验收：读 [验收计划](docs/plans/03-validation.md) 及原始技术设计相应章节。
- 付费调用：遵守验收计划的 30 元项目总上限；记账、预留支出和状态未知任务均计入限额。

## 宿主与数据

只使用独立生成的测试文档，保留用户既有文档及未保存内容。源图、本地配置、缓存和原始测试记录在本地。测试提交仅包含生成素材的代码和脱敏结果。

用户没有授权第三方素材托管。Cangyuan 输入协议由 #7 验证；只读文档不是 multipart 或 base64 已兼容的证据。

按用户 2026-09-17 最新安排，继续 UXP，暂停本机加载排查，先完成代码、本地测试与打包，再补真实宿主验收。浏览器、Node.js、ExtendScript 和真实 UXP 的证据分别记录；真实宿主/付费模型未验收的 Issue 保持未完成。此顺序调整不降低发布条件，详见范围与路线图。
