# Photoshop AI 插件 v0.1 UI 原型文档

**文档版本：** 1.0　 **编写日期：** 2026-09-17  
**对应技术设计：** `01-technical-design.md`  
**可点击演示：** `03-interactive-prototype.html`

> 原型用于确认布局、状态和交互，不连接 Photoshop，不调用模型，不发送或保存真实密钥。演示图片是界面内置的抽象图形，颜色变化不是模型生成结果。正式版本只开放整层修改，选区与蒙版入口标记后续版本。

---

## 1. 设计目标

用户能一眼确认三个问题：**这次改哪个图层、用哪个模型、点击后会发生什么。**

主界面围绕一条操作路径设计：选择源图层 → 输入修改要求 → 生成候选 → 检查结果 → 应用。它是图像编辑工具，不是开放式聊天窗口；v0.1 不做聊天气泡、消息列表或“自动操作全部 PS”的入口。

安全行为必须可见：生成前显示发送对象与服务商；生成中固定任务目标；应用前说明新建结果层、隐藏但保留原层；完成后显示撤销方法。

## 2. 产品信息架构

```text
插件面板
├─ 编辑页（默认）
│  ├─ 标题与设置入口
│  ├─ 模式：整层 / 选区 v0.2 / 蒙版 v0.3
│  ├─ 源图层卡片
│  ├─ 修改要求
│  ├─ 活动模型摘要
│  ├─ 透明度选项
│  ├─ 任务状态或结果预览
│  └─ 固定底部主操作
├─ 设置页（替换当前页，不与编辑页挤在一起）
│  ├─ 本地服务与配对
│  ├─ 模型协议、地址、模型、密钥
│  ├─ 配置检查、付费测试
│  └─ 保存并返回
├─ 导出结果（系统保存文件对话框）
└─ 风险确认 / 错误说明（少量必要对话框）
```

v0.1 不做独立历史页。只保留本会话当前任务与候选；用户重新生成前，应知道会替换当前候选，必要时先导出。

## 3. 布局与视觉规范

### 3.1 尺寸

| 项目 | 规格 |
|---|---|
| 面板基准宽度 | 360 px |
| 最小验收宽度 | 320 px |
| 浮动面板验收宽度 | 420 px |
| 基准高度 | 760 px，可垂直滚动 |
| 最小高度验收 | 520 px；正文滚动，底部主操作保留 |
| 水平边距 | 16 px；320 px 宽时可缩至 12 px |
| 组件间距 | 8 / 12 / 16 / 24 px |
| 控件高度 | 常用输入与按钮 36–40 px |
| 提示词输入区 | 最低 112 px，高度可增长但不占满整屏 |
| 结果预览 | 宽度撑满内容区，比例容器内 contain，不裁切主体 |

插件标题与底部主操作固定，中间内容单独滚动。不能靠把字体缩到 10 px 来适配窄面板；长图层名省略并能查看完整信息。

### 3.2 设计 Token（项目建议）

```css
--bg: #202124;
--surface: #292b30;
--surface-raised: #32353c;
--border: #484d57;
--text: #f4f5f7;
--muted: #b5bdca;
--accent: #96b8ff;
--accent-ink: #101b32;
--success: #90d6b4;
--warning: #f2c780;
--error: #f2a1a7;
--radius-card: 12px;
--radius-control: 8px;
```

界面风格为低饱和深色、清晰层级、适度留白，主要行动使用一种强调色。原生控件优先适配 Photoshop 主题；HTML 演示的浏览器 CSS 只是视觉参考，生产 UXP 必须逐项验证。

标题 18–20 px，区块标题 14 px，正文/控件 13–14 px，辅助文案 12 px。系统字体栈即可，不随包分发字体文件。错误、连接状态不能只用颜色表示，必须同时有文字。

### 3.3 可访问性

所有按钮有可见焦点；图标按钮有名称；Tab 顺序跟随阅读顺序；禁用按钮保留说明；状态变化使用非打断式可访问提示。颜色对比以正文 4.5:1、较大文字 3:1 作为设计验收目标，需在生产主题中测量，不因使用某组色值就假设通过。

不全局抢占 Photoshop 的快捷键。面板“生成”可以支持焦点位于插件内时 `Ctrl/Cmd + Enter`，但不得在文本输入外拦截 PS 常用命令；确认弹窗的 Escape 只关闭当前弹窗。

## 4. P01 编辑页：默认状态

```text
┌──────────────────────────────────┐
│ AI 图层编辑             v0.1  ⚙  │
│ 用文字描述，修改选中的图层         │
├──────────────────────────────────┤
│ [整层修改] [选区 v0.2] [蒙版 v0.3]│
│                                  │
│ 编辑目标                 [刷新]   │
│ ┌──────┐ 几何印花                 │
│ │缩略图│ 包装设计.psd             │
│ └──────┘ 1200 × 800 px · 像素层   │
│ 仅发送此图层，不发送其他图层       │
│                                  │
│ 你想怎么修改？                    │
│ ┌──────────────────────────────┐ │
│ │ 保留构图，把冷色改为暖橙色，  │ │
│ │ 其他细节尽量保持一致。        │ │
│ └──────────────────────────────┘ │
│                                  │
│ 模型                         更改 │
│ 自定义图像模型 · image-edit-model │
│ 状态：图像编辑已验证               │
│                                  │
│ 透明度                            │
│ [保持原图透明轮廓             ▼]  │
│ 不改变原始轮廓与透明区域。         │
│                                  │
│ 新建结果层；原层保留但隐藏。       │
├──────────────────────────────────┤
│ [          生成修改           ]  │
│ 先生成候选，应用前不会改动画布。   │
└──────────────────────────────────┘
```

### 4.1 组件定义

| ID | 组件 | 行为与验收 |
|---|---|---|
| C01 | 标题栏 | 名称固定“AI 图层编辑”；右侧设置图标 |
| C02 | 模式分段控件 | v0.1 只有整层可选；另外两项禁用并带版本 |
| C03 | 目标图层卡 | 显示名称、文档、源尺寸、类型、缩略图 |
| C04 | 刷新目标 | 空闲时读取当前单选层；有候选时确认丢弃 |
| C05 | 修改要求 | 必填；去除首尾空白；项目上限 4000 字符，低于服务商限额时取更小值 |
| C06 | 模型摘要 | 仅显示活动配置；点击“更改”进入设置 |
| C07 | Alpha 下拉 | 保持原轮廓 / 使用模型透明度；按能力禁用选项 |
| C08 | 安全说明 | 应用会新建层、隐藏原层，可一次撤销 |
| C09 | 主要按钮 | 空闲时“生成修改”；状态变化时替换，不出现多个争抢主操作的按钮 |
| C10 | 内联状态 | 与按钮相邻，说明为什么不能生成 |

“刷新”获取目标快照的准备信息；真正提交时再读取并锁定像素快照。未生成前可跟随 PS 选择更新，生成后不得自动跟随。

### 4.2 生成按钮启用条件

本地服务配对成功；活动模型配置完整；恰好一个合格源层；提示词非空；没有活动任务；没有需要用户处理的源状态冲突。

配置未做真实编辑测试时，显示黄色“尚未验证图像编辑”；第一次直接生成前要明确这是未经验证的调用，可能失败并计费。配置格式检查通过不能显示绿色“模型可用”。

### 4.3 整层模式遇到选区

已有 PS 选区时显示：“当前为整层修改，现有选区不会限定编辑范围。”正式实现须保持选区本身不变。不能让用户误以为有蚂蚁线就只修改局部。

## 5. P02 生成中

```text
┌──────────────────────────────────┐
│ AI 图层编辑                  ⚙   │
├──────────────────────────────────┤
│ 任务目标已锁定                   │
│ 几何印花 · 包装设计.psd           │
│                                  │
│ 修改要求（只读）                 │
│ 保留构图，把冷色改成暖橙色。       │
│                                  │
│ ◌ 正在生成候选                   │
│ 已等待 28 秒                     │
│ 画布尚未修改。你可以继续操作 PS。 │
│ 若修改源层，应用前将重新检查。     │
│                                  │
│ 服务：自定义图像模型              │
│ 请求编号：…7c42                  │
├──────────────────────────────────┤
│ [            取消             ]  │
│ 取消不保证上游停止处理或计费。     │
└──────────────────────────────────┘
```

允许显示真实阶段：读取图层、上传输入、等待模型、处理结果。没有上游可测进度时使用不定进度动画与已等待时间，**禁止从 0% 自动涨到 99% 的伪进度**。

设置入口可以查看，但任务期间不可更改活动配置。禁用提示词、模式、目标刷新，防止用户误以为当前任务使用了新输入。

切换文档/图层后显示横幅：“当前 PS 选择已变化，本任务仍编辑‘几何印花’。”这不是立即报错；真正应用时校验源状态。

点击取消后本插件不再应用该任务。若尚未发送，直接结束；已经发送则显示上游取消能力信息。取消后晚到结果不得恢复成正常可应用状态。

## 6. P03 结果就绪

```text
┌──────────────────────────────────┐
│ AI 图层编辑                  ⚙   │
├──────────────────────────────────┤
│ 任务目标：几何印花                │
│                                  │
│ 候选预览         [原图] [结果●]  │
│ ┌──────────────────────────────┐ │
│ │                              │ │
│ │      等比例预览 + 棋盘底      │ │
│ │                              │ │
│ └──────────────────────────────┘ │
│ 回填：1200 × 800 px              │
│ 来源：image-edit-model            │
│                                  │
│ [导出 PNG]         [重新生成]    │
│                                  │
│ 应用将创建新的像素图层，放在原层 │
│ 正上方；原层保留但隐藏。           │
├──────────────────────────────────┤
│ [         应用到画布          ]  │
│ 应用前再次检查目标；可一次撤销。   │
└──────────────────────────────────┘
```

### 6.1 预览规则

原图与结果使用完全相同的显示矩形、缩放和棋盘底。切换不修改 Photoshop。默认显示结果；不要把结果在画布上临时插入再删除来实现“预览”，以免污染历史。

至少显示源尺寸与回填尺寸。若生成时缩小，提示：“模型处理尺寸低于原层，回填会放大；这不是超分。”不要显示“画质无损”这类无法兑现的文案。

不适配 UXP 的拖动分割线和复杂 Canvas 在 v0.1 不实现；原图 / 结果两按钮切换已足够。

### 6.2 重新生成

候选尚未应用时，显示确认：“重新生成会替换当前候选，并发起新的模型请求。需要保留时请先导出。”按钮为“返回预览 / 重新生成”。

重新生成默认使用**同一源快照**与当前提示词；用户改选源层必须显式“重新读取目标”。源已变化时不能默默使用旧快照，先进入冲突说明。

### 6.3 导出

系统文件选择器保存 PNG；默认名 `源图层名-ai-01.png`，清理文件名非法字符。取消保存不是错误，不改变当前任务状态。导出不创建 PSD 图层。

## 7. P04 应用中与成功

### 应用中

显示“正在检查目标…”→“正在应用到 Photoshop…”。主按钮禁用，防止重复应用。不要继续显示“模型生成中”，因为此时不应再调用模型。

若宿主忙，保留候选，显示“Photoshop 正在执行其他命令，稍后可重新应用”；按钮“重新应用”只重复本地写回，不重复生成。

### 成功

```text
┌──────────────────────────────────┐
│ ✓ 已应用                         │
│ 新图层：AI · 几何印花 · 01        │
│ 原图层已保留并隐藏。              │
│                                  │
│ [查看结果图] [导出 PNG]           │
│                                  │
│ [撤销本次应用]                    │
│ 仅当这仍是最新的历史步骤时可用。   │
│                                  │
│ [读取当前选择，开始下一次编辑]     │
└──────────────────────────────────┘
```

完成后不自动发起下一次生成。同一结果显示“已应用”，应用按钮禁用。

用户在应用后又进行了其他 PS 编辑：“撤销本次应用”禁用，说明“后面已有其他编辑，请在 Photoshop 历史面板处理”。插件不能为了撤销 AI 一步而撤掉后面全部操作。

“读取当前选择”沿用 PS 当前选择，不偷偷改选隐藏的源层或新的结果层；若当前选择不合格，进入对应空态。

## 8. P05 设置页

```text
┌──────────────────────────────────┐
│ ‹ 返回                  模型设置 │
├──────────────────────────────────┤
│ 本地服务                         │
│ 地址：127.0.0.1:47833             │
│ 配对 Token [••••••••••••••••]    │
│ [连接本地服务]                   │
│                                  │
│ 模型连接                         │
│ 协议 [OpenAI Images Edits     ▼]  │
│ 配置名称 [自定义图像模型       ]  │
│ API 根地址 [https://…/v1       ]  │
│ 模型名称 [填写服务商模型 ID    ]  │
│ API Key [•••••••••••••••••••• ]  │
│ 密钥仅在本地服务本次会话中保存。   │
│                                  │
│ 请求路径预览：                   │
│ https://…/v1/images/edits         │
│                                  │
│ [检查配置] [发送测试图·可能计费]  │
│ 检查结果：尚未验证图像编辑        │
│                                  │
│ 隐私与缓存                       │
│ 仅发送目标图层与提示词。           │
│ 图片缓存保存在本机，最长 24 小时。 │
├──────────────────────────────────┤
│ [          保存并返回         ]  │
└──────────────────────────────────┘
```

### 8.1 字段细则

| 字段 | 默认/示例 | 校验 |
|---|---|---|
| 本地服务地址 | 固定 127.0.0.1:47833 | v0.1 不提供远程桥接输入 |
| 配对 Token | 空 | 仅本机服务配对，区别于模型 API Key |
| 协议 | OpenAI Images Edits | UI 正式只开放已实现适配器；Mock 仅开发模式可见 |
| 配置名称 | 自定义图像模型 | 1–40 字符，用于摘要显示 |
| API 根地址 | 空 | HTTPS；无用户名密码、无 query/fragment；末尾斜杠统一处理 |
| 模型名称 | 空 | 使用服务商真实模型 ID，不自动假定存在某个模型 |
| API Key | 空/“已设置” | 不从后端回读原值；重新输入才替换 |
| 超时 | 实现默认值，先进阶折叠区 | 项目可配置，不承诺到时一定能停止上游 |

真实密钥不放在示例、截图、提示词和错误详情里。不用“记住密码”开关暗示已经实现系统加密保存；v0.1 明确重启本地服务要重填。

### 8.2 检查与测试的文案层级

1. “格式检查通过”：只校验输入字段。
2. “连接 / 鉴权通过”：对应非生成探测结果。
3. “图像编辑已验证”：确实发送测试图并取得有效图片。

三者不能混为一个绿色连接点。“发送测试图”先确认可能计费，并说明发送的是插件测试素材，不是当前 PSD。

### 8.3 配置变更

点击返回但有未保存修改，确认“放弃修改 / 继续设置”。任务生成中禁止保存会改变其配置的设置；任务已使用的配置版本保持不变。

保存成功后清空 Key 输入值，摘要只显示“凭据已设置”。格式合法但未验证时允许保存，编辑页持续显示未验证状态。

## 9. 空态与异常状态

| 状态 | 标题 / 主文案 | 主操作 | 禁止行为 |
|---|---|---|---|
| 未打开文档 | 先打开一个 Photoshop 文档 | 刷新检测 | 显示虚构图层 |
| 没有单选图层 | 请选择一个普通像素图层 | 刷新目标 | 随意挑第一层 |
| 图层不支持 | 显示具体属性，如“带蒙版” | 重新选择 | 自动栅格化或合并 |
| 空图层 | 此图层没有可编辑像素 | 刷新目标 | 调用模型生成全新图 |
| 未连接本地服务 | 本地服务尚未连接 | 打开设置 | 开放生成 |
| 未配置模型 | 先添加图像编辑模型 | 配置模型 | 默认使用未知付费服务 |
| 图像过大 | 超出本版本单层处理上限 | 查看限制 | 偷偷缩小原稿 |
| 接口失败 | 请求失败，画布未修改 | 检查设置 / 手动重试 | 无限自动重试 |
| 上游状态不明 | 请求状态未知，可能仍在处理 | 处理提示后重试 | 宣称已停止计费 |
| 源图层变化 | 原目标已变化，暂不能应用 | 导出 PNG / 重新读取 | 强行按旧坐标应用 |
| 文档已关闭 | 原文档已关闭 | 导出 PNG | 根据同名文件猜目标 |
| 写回失败 | 应用失败；显示回滚结果 | 重新应用 | 重新调用模型 |
| 服务断开 | 本地服务连接已断开 | 重新连接 | 丢失提示词或声称任务成功 |

错误详情折叠显示错误码、请求 ID、建议动作；不直接把接口返回的 HTML、大量堆栈或密钥显示给用户。

## 10. UI 状态表与按钮规则

| UI 状态 | Prompt | 刷新源层 | 生成 | 应用 | 取消 | 导出 |
|---|---|---|---|---|---|---|
| EMPTY | 可填写保留草稿 | 可用 | 禁用 | 隐藏 | 隐藏 | 隐藏 |
| DRAFT | 可编辑 | 可用 | 满足条件可用 | 隐藏 | 隐藏 | 隐藏 |
| CAPTURING / GENERATING | 只读 | 禁用 | 禁用 | 隐藏 | 可用 | 隐藏 |
| RESULT_READY | 保留原要求 | 确认后可用 | 作为重新生成 | 可用 | 隐藏 | 可用 |
| VALIDATING / APPLYING | 只读 | 禁用 | 禁用 | 禁用 | 不提供假取消 | 可暂时禁用 |
| APPLIED | 当前任务只读 | 开始下一次可用 | 隐藏 | 已应用 | 隐藏 | 可用 |
| CONFLICT | 保留 | 确认后可用 | 新任务路径 | 禁用 | 隐藏 | 有结果可用 |
| ERROR | 保留 | 可用 | 按错误决定 | 有候选且未损坏时重试 | 隐藏 | 有结果可用 |
| DISCONNECTED | 保留 | 可本地读取 | 禁用 | 不具备缓冲时禁用 | 说明只能停止本地等待 | 缓冲可用时开放 |

所有文案和按钮状态由同一 Store 推导，禁止页面 A 认为“已取消”、页面 B 仍然展示“应用到画布”。

## 11. 必要确认对话框

**D01 首次远端发送**  
标题：“发送此图层给模型？”  
正文：“将发送‘几何印花’的图像和修改要求至〔实际域名〕。不会发送其他图层或 PSD 文件。此次请求可能产生费用。”  
操作：“返回检查 / 确认发送”。可按配置版本记住确认，但域名变化后必须重新确认。

**D02 放弃候选**  
标题：“替换当前候选？”  
正文：“当前候选尚未应用。重新生成会替换它，并发起新的模型请求。”  
操作：“保留候选 / 重新生成”。

**D03 付费测试**  
标题：“发送图像编辑测试？”  
正文：“使用内置测试素材验证此模型。服务商可能计费。”  
操作：“取消 / 发送测试图”。

**D04 源状态冲突**  
以面板内联提示为主，不用循环弹窗。显示“结果已保留，但不能应用到变化后的源层”；提供导出与重新读取。

**D05 状态未知后的重试**  
标题：“重新发送可能重复计费”  
正文：“上一请求的上游状态尚未确认。重新发送将创建新请求。”  
操作：“暂不重试 / 仍然重新发送”。

## 12. v0.2 / v0.3 预留 UI

这些仅为后续设计，不在 v0.1 实现，也不允许禁用按钮点击后偷偷执行整层修改。

### 选区修改 v0.2

源层卡下增加“选区范围”和预览叠色；提示“仅修改高亮区域”。上下文扩展是独立参数，不等于扩大最终可修改范围。界面显示原选区面积/范围、上下文边距、边缘策略。

### 蒙版修改 v0.3

源层卡下增加“编辑遮罩来源”：选择图层蒙版或导入灰度文件；并排显示原图、遮罩、叠加预览。白色修改、黑色保护；提供显式反转。尺寸不匹配时不自动拉伸，先提示匹配或重新选择。

“严格范围保护”应是默认工程保证，不包装成模型更听话的付费强度选项。羽化会改变边界处的混合权重，应把变化范围可视化，不能一边扩张羽化一边承诺原始选区外绝对不变。技术细节见技术设计第 13 节。

## 13. 可点击 HTML 原型说明

打开 `03-interactive-prototype.html`，直接在浏览器中查看。不需要安装依赖，无外部 CDN、字体、网络请求。

演示覆盖：整层模式、编辑目标、提示词、生成等待、候选切换、应用、新旧图层显隐、撤销、导出示意、设置、配置检查、未打开文档、未配置、源层冲突、应用失败、服务断开。

页面顶部的“状态演示”和“面板宽度”是评审控件，**不属于正式插件 UI**。工作区与图层列表也是宿主示意，不是额外开发的 Photoshop 替代界面。

原型中的模型配置是示例值；Key 输入不会持久化，切勿填写真实凭据。测试图、生成耗时、应用和撤销都是本地模拟。浏览器截图只验证布局，不证明 Photoshop API 或真实模型可用。

## 14. UI 验收清单

| 编号 | 要求 |
|---|---|
| U01 | 320/360/420 px 宽度无横向溢出，主按钮和错误提示可见 |
| U02 | 520 px 高度时中间内容可滚动，底部主操作没有遮挡末尾字段 |
| U03 | 用户能在生成前看清源层、文档、模型、发送范围 |
| U04 | 输入为空、目标不支持、服务未连接时均不能生成 |
| U05 | 生成后切换 PS 选择，不会改变任务目标卡片 |
| U06 | 无真实进度时不显示假百分比 |
| U07 | 未点击应用前，宿主图层数与源层显隐不变 |
| U08 | 应用成功后结果显示、源层保留且隐藏 |
| U09 | 撤销资格检查与技术设计一致，不撤回后续用户动作 |
| U10 | 三种连接检查有不同准确文案，无“已保存=已验证”误导 |
| U11 | 未来入口不可用，并标明版本，不伪装成当前功能 |
| U12 | 原图/结果预览尺寸一致，透明背景与边缘可辨认 |
| U13 | 源冲突和写入失败时保留候选，不诱发重复模型费用 |
| U14 | 关键操作支持键盘焦点，错误不仅依赖颜色 |
| U15 | 截图、日志、Key 回读、浏览器存储中无真实凭据 |

## 15. 给 UI 开发者的交付说明

先实现 P01、P02、P03、P04、P05 及状态表，不新增聊天气泡、全自动 Agent、Strength、批量历史等额外模块。先用 Mock 接通状态，接口契约以技术设计为准。

原型字体、颜色、间距是推荐视觉起点；信息优先级、交互状态、风险提示和禁用规则是必须实现的功能要求。生产界面应在真实 UXP 宿主里逐项验证 HTML/CSS 与事件行为，不直接把浏览器页面打包成插件就交付。

### 参考依据

宿主能力、网络权限与回填边界来源见技术设计 [A1]–[A8]。本文件的布局、文案、尺寸、验收状态均为本项目原型设计，不代表 Adobe 官方界面规范。
